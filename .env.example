# Copy to .env and fill in values suitable for your local setup
DB_NAME=postgres
DB_USER=odoo
DB_PASSWORD=odoo
# Branch for ./addons_my when cloning/updating (defaults to main if not set)
ADDONS_MY_BRANCH=main
